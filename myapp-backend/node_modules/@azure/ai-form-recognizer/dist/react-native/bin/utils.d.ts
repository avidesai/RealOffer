/**
 * Convert a list of strings into a camelCase joined representation.
 */
export declare function camelCase(slug: string[]): string;
/**
 * Capitalize a string.
 */
export declare function capitalize(s: string): string;
/**
 * Uncapitalize a string.
 */
export declare function uncapitalize(s: string): string;
/**
 * A simple representation of a field in an interface.
 */
export interface Field {
    name: string;
    docContents: string;
    type: string;
    optional?: true;
}
//# sourceMappingURL=utils.d.ts.map